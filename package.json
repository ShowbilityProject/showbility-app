{
  "name": "showbility",
  "version": "1.0.0",
  "scripts": {
    "app": "pnpm -F @showbility/app",
    "api": "dotenv -- pnpm -F @showbility/api",
    "dev": "dotenv -- pnpm -r --stream --parallel dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.3.3"
  },
  "private": true,
  "packageManager": "pnpm@9.9.0"
}
